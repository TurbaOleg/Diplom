<!DOCTYPE html>
<html>
	{{template "view/lib/head" "<title>Cookies</title>"}}
<body>
	{{template "view/lib/header" "cookies"}}
<div class=container>
	<div class=row>
	<div class="col-4 overflow-scroll" style=height:100vh>
	{{template "view/lib/domains" .domains}}
	</div>
	<div class="col-8 overflow-scroll" style=height:100vh>
	{{template "view/lib/cookies" .cookies}}
	</div>
	</div>
</div>
</body>
</html>
